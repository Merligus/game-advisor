pandas==2.3.3
ipykernel==7.1.0
requests==2.32.5
python-dotenv==1.2.1
howlongtobeatpy==1.0.22  # 1.0.19 silently returns 0 results vs current HLTB site
rawg==1.2.0
thefuzz==0.22.1
scikit-learn==1.8.0
sentence-transformers==5.2.3
d3rlpy==2.8.1
gradio==6.15.1
curl_cffi==0.15.0
# pip install playwright && playwright install chromium in case of gamespot API problem
# torch is installed separately (CUDA build varies by host); this repo was
# trained on torch 2.11.0+cu130 with an RTX 3050 6GB Laptop GPU.